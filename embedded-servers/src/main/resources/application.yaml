spring:
  application:
    name: embedded-servers

server:
  port: 8090   # sabit: order-service broker adresini bu porttaki /kafka/brokers'tan okur

logging:
  level:
    org.apache.kafka.clients.admin.internals.AdminMetadataManager: WARN
