openapi: 3.0.3
info:
  title: Product Service API V2
  version: 1.0.0
  description: Product microservice contract (contract-first).

servers:
  - url: http://localhost:8081

tags:
  - name: ProductsV2
    description: Product V2 management endpoints

paths:
  /api/v2/products:
    get:
      tags: [ProductsV2]
      summary: List products
      operationId: listProducts
      security: []
      parameters:
        - name: page
          in: query
          required: false
          description: 0-based page index
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: size
          in: query
          required: false
          description: Page size
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
        - name: q
          in: query
          required: false
          description: Optional search by name or sku (implementation-defined)
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Paged product list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductPageV2"
        "400":
          description: Invalid query parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

    post:
      tags: [ProductsV2]
      summary: Create product
      operationId: createProduct
      
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductCreateRequestV2"
      responses:
        "201":
          description: Created
          headers:
            Location:
              description: URL of created resource
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductV2"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"
        "409":
          description: SKU already exists (unique constraint)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

  /api/v2/products/{id}:
    get:
      tags: [ProductsV2]
      summary: Get product by id
      operationId: getProductById
      
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductV2"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

    put:
      tags: [ProductsV2]
      summary: Replace product by id
      operationId: replaceProduct
      
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductUpdateRequestV2"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductV2"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"
        "409":
          description: SKU already exists (unique constraint)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

    delete:
      tags: [ProductsV2]
      summary: Delete product by id
      operationId: deleteProduct
      
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

  /api/v2/products/update-stock:
    put:
      tags: [ProductsV2]
      summary: Update product stock
      operationId: updateStock
      
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StockUpdateRequestV2"
      responses:
        "200":
          description: Stock updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockUpdateResponseV2"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"
        "404":
          description: Product not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponseV2"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ProductV2:
      type: object
      required: [id, name, price, stock, sku]
      properties:
        id:
          type: string
          format: uuid
          description: Product identifier (UUID)
        name:
          type: string
          minLength: 1
          maxLength: 200
        price:
          type: number
          format: double
          minimum: 0
          exclusiveMinimum: true
          description: Must be > 0
        stock:
          type: integer
          format: int32
          minimum: 1
          description: Must be a positive integer (> 0)
        sku:
          type: string
          minLength: 1
          maxLength: 64
          description: Stock code (unique). Enforced by storage layer; conflicts return 409.

    ProductCreateRequestV2:
      type: object
      required: [name, price, stock, sku, image_url]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        price:
          type: number
          format: double
          minimum: 0
          exclusiveMinimum: true
        stock:
          type: integer
          format: int32
          minimum: 0
        sku:
          type: string
          minLength: 1
          maxLength: 64
        image_url:
          type: string

    ProductUpdateRequestV2:
      type: object
      required: [name, price, stock, sku]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        price:
          type: number
          format: double
          minimum: 0
          exclusiveMinimum: true
        stock:
          type: integer
          format: int32
          minimum: 1
        sku:
          type: string
          minLength: 1
          maxLength: 64

    ProductPatchRequestV2:
      type: object
      additionalProperties: false
      description: Any subset of fields can be provided. Validation still applies when present.
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        price:
          type: number
          format: double
          minimum: 0
          exclusiveMinimum: true
        stock:
          type: integer
          format: int32
          minimum: 1
        sku:
          type: string
          minLength: 1
          maxLength: 64

    ProductPageV2:
      type: object
      required: [items, page, size, totalItems, totalPages]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ProductV2"
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalItems:
          type: integer
          minimum: 0
        totalPages:
          type: integer
          minimum: 0

    ErrorResponseV2:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: price must be greater than 0
        details:
          type: array
          items:
            type: string
          description: Optional list of field-level messages

    StockUpdateRequestV2:
      type: object
      required: [id, stock]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        stock:
          type: integer
          format: int32
          minimum: 0

    StockUpdateResponseV2:
      type: object
      required: [id, name, stock]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        stock:
          type: integer
          format: int32